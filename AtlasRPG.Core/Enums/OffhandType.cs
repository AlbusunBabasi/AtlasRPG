namespace AtlasRPG.Core.Enums
{
    public enum OffhandType
    {
        Shield = 1,  // Block + armor → Warrior/Berserker
        Quiver = 2,  // Attack speed + accuracy → Archer
        Focus = 3   // Mana + ward + spell → Mage
    }
}
